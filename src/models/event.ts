export class Event{
    title: string;
    note:string;
    icon:string;

    constructor(title, note, icon){
        this.title = title;
        this.note = note;
        this.icon = icon;
        
    }
}